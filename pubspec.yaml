name: orderadd
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  velocity_x: ^3.6.0
  line_icons: ^2.0.1
  firebase_auth: ^4.2.0
  device_preview: ^1.1.0
  go_router: ^5.2.2
  url_strategy: ^0.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/persons/
