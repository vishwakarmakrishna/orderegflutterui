name: orderadd
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  vrouter: ^1.2.0+12
  velocity_x: ^3.3.0
  line_icons: ^2.0.1
  firebase_auth_web:
  firebase_auth: ^3.1.1
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/persons/
